---
- hosts: all
  tasks:
    - name: "Instalar pip3"
      apt:
        name: "{{ item }}"
        state: latest
      with_items:
        - python3-pymysql
        - python3-pip
    - name: "Criar BD mySQL"
      community.mysql.mysql_db:
        name: wordpress_db
        state: present
